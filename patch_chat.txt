s/val ttsManager = remember { TtsManager(context) }/val ttsManager = remember { NvidiaTtsManager(context) }/g
s/ttsManager.speak(\n                lastMessage.messageId,\n                lastMessage.content,\n                currentPersona.gender,\n                currentPersona.relationship,\n                currentPersona.name/ttsManager.speak(\n                lastMessage.messageId,\n                lastMessage.content,\n                currentPersona.voice/g
s/ttsManager.speak(\n                                    msg.messageId,\n                                    msg.content,\n                                    currentPersona.gender,\n                                    currentPersona.relationship,\n                                    currentPersona.name/ttsManager.speak(\n                                    msg.messageId,\n                                    msg.content,\n                                    currentPersona.voice/g
